# Environment

Create a `.env.local` (never commit it) with:

```
RESEND_API_KEY=your_resend_api_key
CONTACT_TO=info@varatek.fi
CONTACT_FROM=no-reply@varatek.fi
```

Deploy: set the same variables in your hosting provider (e.g., Vercel → Project → Settings → Environment Variables).

